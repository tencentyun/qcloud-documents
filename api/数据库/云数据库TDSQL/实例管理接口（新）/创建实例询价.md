## 1. 接口描述
本接口（QueryTdsqlCreateInstancePrice）用于查询创建实例的价格。
接口请求域名：**_tdsql.api.qcloud.com_**

## 2. 输入参数
以下请求参数列表仅列出了接口请求参数，正式调用时需要加上公共请求参数，见[公共请求参数](/doc/api/309/7016)页面。
其中，此接口的 Action 字段为 QueryTdsqlCreateInstancePrice。

| 参数名称| 类型| 是否必选| 描述|
|---------|---------|---------|---------|
| Version|String|是|表示 API 版本号，主要用于标识请求的不同 API 版本。 本接口第一版本可传：2017-03-12。|
| Zone | String|是|实例所属的可用区ID。|
| CostType | Integer | 否 | 计费类型：0 包年包月，1 按量计费。目前仅支持包年包月。|
| Pid | Integer|是|产品类型 Id，通过[查询实例规格](/doc/api/309/5537)获取。|
|GoodsNum|Integer|是|商品数，不传则默认为 1|
|Period|Integer|否|时长，单位：月。查询包年包月类型时有效。|
|Memory|Integer|是|内存大小，单位：MB|
|Storage|Integer|是|存储空间大小，单位：MB|

## 3. 输出参数
| 参数名称 | 类型 | 描述 |
|---------|---------|---------|
| RequestId| String| 唯一请求 ID。每次请求都会返回 `RequestId`。当用户调用接口失败找后台研发人员处理时需提供该 `RequestId`。|
| OriginalPrice| Integer | 原价，单位：分|
| Price | Integer |实际价格，单位：分。受折扣等影响，可能和原价不同 |

## 4. 错误码表
以下是本接口常见的错误码，如果有不在此列的错误请查阅 [TDSQL 错误码表](/doc/api/309/7150)

| 错误码 | 描述 |
|---------|---------|
| InvalidParameter.NotSupportedPayMode | 不支持的计费类型，当前仅支持包年包月 |
| QueryPriceFailed | 查询价格失败 |

## 5. 示例
输入
<pre>
https://tdsql.api.qcloud.com/v2/index.php?Action=QueryTdsqlCreateInstancePrice
&Version=2017-03-12
&<<a href="https://cloud.tencent.com/doc/api/229/6976">公共请求参数</a>>
&Pid=10890
&GoodsNum=1
&Period=1
&Memory=2000
&Storage=10000
&Zone=ap-guangzhou-1
</pre>
输出
```
{
    "Response": {
        "RequestId": "7e1000c2-190a-d0df-ff75-59fbdf5ff381",
        "OriginalPrice": 21120,
        "Price": 21120
    }
}
```

