If the API is successfully called, the error code is 0, the error message is empty, and the returned result data is displayed.
Example:
```
{
    "code": 0,
    "message": "",
    <Returned result data>
}
```