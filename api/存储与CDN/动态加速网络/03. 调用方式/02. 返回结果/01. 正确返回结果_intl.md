If the API call succeeds, the error code in the returned result is 0, the error message field is left empty, codeDesc is Success, and the returned result is displayed.

Example:

```
{
		"code":0,
		"message":"",
        "codeDesc": "Success",
		<Returned result>
}
```

