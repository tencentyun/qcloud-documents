If an API call succeeds, the error code in the returned result is 0, the error message field is empty, and the returned data result is displayed.
Example:
```
{
    "code": 0,
    "message": "",
    <Returned result data>
}
```
