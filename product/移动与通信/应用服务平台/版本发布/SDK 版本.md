## tcb-admin-node

### v1.4.2
- 修复了 doc.set() 对于复杂错误类型的签名错误问题。
- db.serverDate() 支持 new 调用。

### v1.4.1
- 修复了下载文件接口如果文件名称中有中文会失败的 bug。

### v1.4.0
- 接口加入默认超时时间（15秒）。
- 对超过3秒的数据库慢查询，加入 console.warn。
- 修复 serverDate 存取的问题。

### v1.3.0
- 重构了 command 的实现。

### v1.2.2
- 修复 GeoPoint 的查询、储存问题。
- 修复 db.RegExp 不支持 or 的问题。
- 修复 Date 不能使用 query 查询的问题。

### v1.2.1
- 查询支持 db.RegExp。
- 修复 Date 对象的底层表示，与微信对齐。

### v1.1.8
-  修复操作符嵌套报错的问题。

### v1.1.7
-  正则表达式支持传入 flags。

### v1.1.6
- 查询指令支持正则表达式。

### v1.1.5
- 修复了逻辑操作符嵌套使用会报错的 bug。
- 修复了 update 嵌套对象部分生效的 bug。

### v1.1.3
- 修复了serverDate传入参数时的 bug。

### v1.1.3
- 修复了数据库读取 serverDate 结构的问题。

### v1.1.2
-  修复了数据库 set 的多维对象的操作问题。

### v1.1.0
- 支持使用多个环境。


### v1.0.31

- 云函数调用返回的 requestId 可以在云控制台用来查看日志。
- 数据库地理位置初始化时第一个参数为 longitude，第二个为 latitude。
- 新增条件删除文档接口。

### v1.0.29
更新文档。

### v1.0.24
获取文件下载链接方法参数变更。

### v1.0.23
- 修复了新增内嵌文档的 bug。

### v1.0.22
-  update 和 set 传空参数会报错，不再抛出异常。
-  init 可以传空。
-  云函数的调用，云函数实际返回的结果从字符串改成了对象，也就是透传云函数返回的结果。

### v1.0.21
-  update 内嵌文档的操作符使用。

### v1.0.20
- 修改了集合创建的方法。
- 修正了环境 ID 的传入 bug。

### v1.0.19
- 增加了新增集合方法。
- 增加了文件下载方法。

### v1.0.18
- 数据库操作的 field() 方法后需要 get() 才能取得数据。
- 添加了数据库的 serverDate 数据类型。
- 增加了数据库的更新 (update) 方法的数组操作符 push、pop、shift 和 unshift，增加了 set 指令。
- 数据库逻辑运算符 and 和 or 支持传入一个数字，作为逻辑运算的参数。


### v1.0.17
- 对象初始化实例后，init操作可以传入空参数，这样会使用默认环境。如果需要指定环境 (env) 或者代理(proxy)，则还是通过 init 方法传入。
-  init 时跟小程序 SDK 保持参数命名一致，envName 改为 env。
-  init 时 mpAppId 不再需要传入。
-  修复了数据库排序的 bug。
-  增加了数据库的 count 方法。
-  修正了文档，修改了文件存储和云函数的返回结果。


### v1.0.9
- 云函数内使用不需要填写 secretId 和 secretKey，云函数重新部署后生效。
- 文件上传添加支持 buffer。
- 修复了嵌套对象的 bug。



## tcb-js-sdk
### v1.0.0
初始版本，支持云函数、数据库、文件存储和登录认证的云开发基础能力。

