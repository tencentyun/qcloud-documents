当客户端用户需要访问云剪相关接口时，为简化开发者的工作，云剪提供了客户端 API 以及配套的 SDK。客户端 API 的调用方式如下所示：

![](https://main.qcloudimg.com/raw/faa3b0323670a63f390177f44677962b.png)

1. 开发者客户端向开发者服务端发起请求，申请客户端访问签名。
2. 开发者服务端在对客户端进行鉴权之后，生成客户端访问签名（其 `action` 字段值为 `Login`），并返回给客户端。参见 [签名算法](/document/product/1156/43777) 及 [签名示例](/document/product/1156/43778)。
3. 开发者客户端使用服务端返回的签名初始化客户端 SDK 的 Net 模块。
4. 使用客户端 SDK 的 Net 组件调用 [客户端 API](TODO: 20-API 概览.md)。




## 使用客户端 SDK 的 Net 组件访问客户端 API
### Web 端

### Step 1：初始化 SDK 
在您的 Web 页面中引入 SDK 资源文件：
```html
<script src="https://imgcache.qq.com/qcloud/cme/sdk_v1.js"></script>
```

引入完成后，全局将挂载一个 `CME` 实例。

### Step 2：获取客户端访问签名
开发者前端向自身服务端请求客户端访问签名（其 `action` 字段值为 `Login`），参见 [签名算法](/document/product/1156/43777) 及 [签名示例](/document/product/1156/43778)。


### Step 3：创建 Net 实例
```js
/* @create  创建实例方法。没调用该方法之前。调用 fetch 方法通信无效。
 *    @param sign {{ string }}，客户端访问签名（其 `action` 字段值为 `Login`）。参见签名算法。
 *    @return 返回一个 Net 组件实例。
 **/
let myCmeNet = CME.Net.create({
  sign: "your_signature",
});
```


### Step 4：发起客户端 API 访问调用
```js
  /**
   *
   * @send 发送命令字方法,
   *    @param path {{string}} 字符串 WEB 路径,参考 API 概览。
   *    @param param {{object}} 命令入参。
   *    @return Promise {{function}} 完成命令的回调函数。
   *
   **/
  let result = myCmeNet.fetch("/path/to/a/action", {
    k1 : 'v1',
	k2 : 'v2'
  });

  result.then((res)=>{
    /***
     * res
     * 响应结果
     * */

  }).catch(err=>{
    /**
     * err
     * 异常信息抛出
     * */
  })
```



## 通过 HTTP 请求直接调用客户端 API
>! 如非必要，建议您使用客户端 SDK 的 Net 组件访问客户端 API，而非通过如下 HTTP 请求直接裸调接口

#### Step 1：获取客户端访问签名（Signature）
开发者前端向自身服务端请求客户端访问签名（其 `action` 字段值为 `Login`），参见 [签名算法](/document/product/1156/43777) 及 [签名示例](/document/product/1156/43778)。

#### Step 2：发起登录，换取长期票据
TODO

#### Step 3：使用 HTTP POST 调用客户端 API
TODO