>!新版自定义监控目前为灰度测试阶段，如需使用请联系您的客户经理或 [提交工单](https://console.cloud.tencent.com/workorder/category) 处理。

自定义监控支持 [CLI]() 和 [SDK]() 两种方式上报监控指标数据，本文将向您介绍上报接口和详细的数据上报步骤。

## 上报接口说明

#### 接口描述

| 接口描述             | 接口说明                                                     |
| -------------------- | ------------------------------------------------------------ |
| 接口请求域名         | monitor.tencentcloudapi.com                                  |
| 默认接口请求频率限制 | 50次/秒                                                      |
| 上报指标限制         | 单次上报最多 30 个指标/值对，请求返回错误时，请求中所有的指标/值均不会被保存。 |
| 默认指标总数         | 100个/租户                                                   |
| 上报的时间戳         | 期望保存的时间戳，建议构造整数分钟时刻的时间戳。 时间戳时间范围必须为当前时间到 300 秒前之间。 同一 IP 指标对的数 据需按分钟先后顺序上报。 |

#### 输入参数

以下请求参数列表仅列出了接口请求参数和部分公共参数，完整公共参数列表见 [公共请求参数](https://cloud.tencent.com/document/product/628/18814)。

| 参数名称          | 必选 | 类型                 | 描述                                                         |
| ----------------- | ---- | -------------------- | :----------------------------------------------------------- |
| Action            | 是   | String               | 公共参数，本接口取值：PutMonitorData                         |
| Version           | 是   | String               | 公共参数，本接口取值：2018-07-24                             |
| Region            | 是   | String               | 公共参数，腾讯云设备报的地域信息（单击 [这里](http://metadata.tencentyun.com/latest/meta-data/placement/region) 获取），查询可用 [地域列表]() |
| Metrics.N         | 是   | Array of MetricDatum | 一组指标和数据                                               |
| AnnounceInstance  | 否   | String               | 上报实例名或 IP（IP 单击 [这里](http://metadata.tencentyun.com/latest/meta-data/local-ipv4) 获取)，最大长度128字节 |
| AnnounceTimestamp | 否   | Integer              | 上报时自行指定的时间戳，例如 1572438660                      |

其中，MetricDatum参数说明如下：

| 参数名称   | 必选 | 类型    | 描述                                                 |
| ---------- | ---- | ------- | ---------------------------------------------------- |
| MetricName | 是   | String  | 指标名称,最大长度255字节, 只允许A-Za-z0-9\-_字符匹配 |
| Value      | 是   | Integer | 指标值，整数，不得超过32字节，超过32字节会被截断     |

#### 输出参数

| 参数名称  | 类型   | 描述参数名称                                                 |
| --------- | ------ | ------------------------------------------------------------ |
| RequestId | String | 唯一请求 ID，每次请求都会返回。定位问题时需要提供该次请求的 RequestId |



#### 地域列表
本产品 Region 字段可选值如下表所示：

| 区域                 | 取值              |
| -------------------- | ----------------- |
| 亚太东南(曼谷)       | ap-bangkok        |
| 华北地区(北京)       | ap-beijing        |
| 西南地区(成都)       | ap-chengdu        |
| 西南地区(重庆)       | ap-chongqing      |
| 华南地区(广州)       | ap-guangzhou      |
| 华南地区(广州Open)   | ap-guangzhou-open |
| 港澳台地区(中国香港) | ap-hongkong       |
| 亚太南部(孟买)       | ap-mumbai         |
| 亚太东北(首尔)       | ap-seoul          |
| 华东地区(上海)       | ap-shanghai       |
| 华东地区(上海金融)   | ap-shanghai-fsi   |
| 华南地区(深圳金融)   | ap-shenzhen-fsi   |
| 亚太东南(新加坡)     | ap-singapore      |
| 亚太东北(东京)       | ap-tokyo          |
| 欧洲地区(法兰克福)   | eu-frankfurt      |
| 欧洲地区(莫斯科)     | eu-moscow         |
| 美国东部(弗吉尼亚)   | na-ashburn        |
| 美国西部(硅谷)       | na-siliconvalley  |
| 北美地区(多伦多)     | na-toronto        |

#### 错误码

以下仅列出了接口业务逻辑相关的错误码，其他错误码详见 [公共错误码](https://cloud.tencent.com/document/product/628/18822)。

| 错误码                                 | 描述                                                        |
| -------------------------------------- | ----------------------------------------------------------- |
| DryRunOperation                        | DryRun 操作，代表请求将会是成功的，只是多传了 DryRun 参数。 |
| FailedOperation                        | 操作失败。                                                  |
| InternalError                          | 内部错误。                                                  |
| InternalError.ExeTimeout               | 执行超时。                                                  |
| InvalidParameter                       | 参数错误。                                                  |
| InvalidParameter.InvalidParameter      | 参数错误。                                                  |
| InvalidParameter.InvalidParameterParam | 参数错误。                                                  |
| InvalidParameterValue                  | 无效的参数值。                                              |
| MissingParameter                       | 缺少参数错误。                                              |
| ResourceNotFound                       | 资源不存在。                                                |
| ResourceUnavailable                    | 资源不可用。                                                |
| UnauthorizedOperation                  | 未授权操作。                                                |
| UnknownParameter                       | 未知参数错误。                                              |
| UnsupportedOperation                   | 操作不支持。                                                |



## CLI 方式上报数据

1. [安装 TCCLI 工具](https://cloud.tencent.com/document/product/440/34011)。
2. 配置 TCCLI：执行以下命令，分别配置 secretId、secretKey、region 和 output 格式。
```
tccli configure set secretId   您的 secretId
# tccli configure set secretId 0000000
tccli configure set secretKey  您的keysecretKey
# tccli configure set secretKey 111111111
tccli configure set region $(curl http://metadata.tencentyun.com/latest/meta-data/placement/region 2>/dev/null) 
tccli configure set output json
```
 - secretId 和 secretKey 可参考 [访问密钥](https://cloud.tencent.com/document/product/598/37140) 获取，下文命令举例使用的 secretId 和 secretKey 为0000000和111111111。
 - region 可执行下文命令直接获取参数值，也可在本文 常用问题 章节查看可选区域的对应取值。
 -  output 使用默认的 JSON 输出格式。
3. 上报监控数据：执行`tccli monitor PutMonitorData`命令进行数据上报。
以下为一个上报示例，您可以直接复制完成上报体验，也可按照您的需要参考 [输入参数说明](#2.-.E8.BE.93.E5.85.A5.E5.8F.82.E6.95.B0) 对参数进行调整后进行自由上报。
```
tccli monitor PutMonitorData \
	--AnnounceInstance $(curl http://metadata.tencentyun.com/latest/meta-data/local-ipv4 2>/dev/null)\
      --AnnounceTimestamp $(date +%s) \
      --Metrics '[{"MetricName": "err_count","Value": 10},{"MetricName": "post_count","Value": 20}, {"MetricName": "users","Value": 30}]'
```

4. 上报成功：如下返回RequestId则说明上报成功，上报成功后系统将自动注册指标，已注册指标不会重复创建。
```sh
{ "RequestId": "dc874995-e065-41a9-9aeb-bb02942d65e3"}
```

5. 您还可以编写 shell 文件调用 TCCLI 工具完成数据上报，如下为一个 shell 文件示例，您还可以参见最佳实践 [监控 Nginx 进程存活](https://cloud.tencent.com/document/product/397/40210) 文档查看如何通过更完整的 shell+CLI 的方式完成数据上报。
```shell
#!/bin/bash
err_count=100
post_count=10000
users=99
metrics=$(cat <<EOF
[
  {
    "MetricName": "err_count", 
    "Value":$err_count 
  }, 
  {
    "MetricName": "post_count", 
    "Value": $post_count
  }, 
  {
    "MetricName": "users", 
    "Value": $users
  }
]
EOF
)
myip=$(curl http://metadata.tencentyun.com/latest/meta-data/local-ipv4 2>>/dev/null)
tccli monitor PutMonitorData --Metrics "$metrics" --AnnounceInstance "$myip"  --AnnounceTimestamp $(date +%s)
```



## SDK 方式上报数据

1. 安装 SDK
   云 API 3.0 提供了配套的开发工具集（SDK），支持多种编程语言，能更方便的调用 API。
   - [Tencent Cloud SDK 3.0 for Python](https://github.com/TencentCloud/tencentcloud-sdk-python)
   - [Tencent Cloud SDK 3.0 for Java](https://github.com/TencentCloud/tencentcloud-sdk-java)
   - [Tencent Cloud SDK 3.0 for PHP](https://github.com/TencentCloud/tencentcloud-sdk-php)
   - [Tencent Cloud SDK 3.0 for Go](https://github.com/TencentCloud/tencentcloud-sdk-go)
   - [Tencent Cloud SDK 3.0 for NodeJS](https://github.com/TencentCloud/tencentcloud-sdk-nodejs)
   - [Tencent Cloud SDK 3.0 for .NET](https://github.com/TencentCloud/tencentcloud-sdk-dotnet)
2. Go 语言 Demo
```go
package main

import (
        "github.com/gogo/protobuf/proto";
        "github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/common";
        "github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/common/profile";
        nm "github.com/tencentcloud/tencentcloud-sdk-go/tencentcloud/monitor/v20180724";
        "log";
        "time";
        "flag";
  			"io/ioutil"
				"net/http"
)

func main() {
				var resp *http.Response
  			resp, _ = http.Get("http://metadata.tencentyun.com/latest/meta-data/placement/region")
        region, _ := ioutil.ReadAll(resp.Body)
        resp, _ = http.Get("http://metadata.tencentyun.com/latest/meta-data/local-ipv4")
  			instance, _ := ioutil.ReadAll(resp.Body)
  
        err_count :=flag.Uint64("err_count",0,"err_count");
        post_count :=flag.Uint64("post_count",0,"post_count");
        users :=flag.Uint64("users",0,"users count");

        flag.Parse();
        if *err_count== 0 || *post_count==0 || *users ==0 {
                flag.Usage();
                return; 
        }
				// 实例化一个认证对象，入参需要传入腾讯云账户secretId，secretKey
				credential := common.NewCredential("secretId", "secretKey")
				// 实例化要请求产品(以monitor为例)的client对象
        cpf := profile.NewClientProfile()
				// cpf.HttpProfile.Endpoint = "monitor.tencentcloudapi.com"
				// （必选项）定义上报机器所在地域,可用地域参考https://cloud.tencent.com/document/api/213/15692文档[地域列表]
        client, err := nm.NewClient(credential, string(region), cpf)

        if err != nil {
                log.Fatal(err)
        }

				req := nm.NewPutMonitorDataRequest()
				// [可选项] 定义上报的服务名或ip,如果不填，将使用请求到云Api接口机最后一跳的外网ip(可能是出口ip或者代理ip)
				req.AnnounceInstance = proto.String(string(instance))
				// [可选项] 定义上报的时间戳，如果不填，将使用服务器系统时间戳
				req.AnnounceTimestamp = proto.Uint64(uint64(time.Now().Unix()))

				// [必选项] 定义指标数据
				req.Metrics = []*nm.MetricDatum{
                {proto.String("err_count"), proto.Uint64(*err_count)},
                {proto.String("post_count"), proto.Uint64(*post_count)},
                {proto.String("users"), proto.Uint64(*users)},
        }
        
        // 发送request请求并响应
        rsp, err := client.PutMonitorData(req)
        if err != nil {
                log.Fatal(err)
        }
        log.Println(rsp.ToJsonString())

}
```

3. Python 语言 Demo

```python
# -*- coding: utf-8 -*-
import time
import urllib2
from tencentcloud.common import credential
from tencentcloud.common.exception.tencent_cloud_sdk_exception import TencentCloudSDKException
from tencentcloud.monitor.v20180724 import monitor_client, models

try:
   # 获取region地区
    url_region = "http://metadata.tencentyun.com/latest/meta-data/placement/region"
    region = urllib2.urlopen( urllib2.Request(url_region) ).read()
    #print(region)
    # 获取实例Ip
    url_ip = "http://metadata.tencentyun.com/latest/meta-data/local-ipv4"
    instance =  urllib2.urlopen( urllib2.Request(url_ip)).read()
    #print(instance)

    cred = credential.Credential("secretId", "secretKey")

    client = monitor_client.MonitorClient(cred, region)

    req = models.PutMonitorDataRequest()

    req.AnnounceInstance = instance
    req.AnnounceTimestamp = int(time.time())
    req.Metrics = [
      {"MetricName": "err_count","Value": 10},
      {"MetricName": "post_count","Value": 20},
      {"MetricName": "users","Value": 30}
    ]
    resp = client.PutMonitorData(req)
    print(resp.to_json_string())

except TencentCloudSDKException as err:
    print(err)
```

4. JAVA 语言 Demo

```java
package com.tencent.monitor;

import com.tencentcloudapi.common.Credential;
import com.tencentcloudapi.common.exception.TencentCloudSDKException;
import com.tencentcloudapi.common.profile.ClientProfile;
// 导入对应产品模块的client
import com.tencentcloudapi.monitor.v20180724.MonitorClient;
// 导入要请求接口对应的request response类
import com.tencentcloudapi.monitor.v20180724.models.PutMonitorDataRequest;
import com.tencentcloudapi.monitor.v20180724.models.PutMonitorDataResponse;
import com.tencentcloudapi.monitor.v20180724.models.MetricDatum;

import java.util.Date;

public class PutMonitorData
{

	public static void main(String [] args) {
		try{
      
			// 实例化一个认证对象，入参需要传入腾讯云账户secretId，secretKey
			Credential cred = new Credential("secretId", "secretKey");

			// 实例化要请求产品(以monitor为例)的client对象
			ClientProfile clientProfile = new ClientProfile();
			clientProfile.setSignMethod(ClientProfile.SIGN_TC3_256);

			// （必选项）定义上报机器所在地域,可用地域参考https://cloud.tencent.com/document/api/213/15692文档[地域列表]
			MonitorClient client = new MonitorClient(cred, "ap-guangzhou", clientProfile);
			PutMonitorDataRequest req = new PutMonitorDataRequest();

			// [可选项] 定义上报的ip(建议本机的内网IP),如果不填，将使用请求到云网关接口机最后一跳的外网ip(可能是出口ip或者代理ip)
			req.setAnnounceInstance( "127.0.0.1" );

			// [可选项] 定义上报的时间戳，如果不填，将使用服务器系统时间戳
			Long ts = new Date().getTime()/1000;
			req.setAnnounceTimestamp( ts.intValue() ); 
			MetricDatum[] metrics = new MetricDatum[3];
			String[] metricsName = new String[]{"err_count","post_count","users"};
			for(int i=0; i<3; i++) {
				metrics[i] = new MetricDatum();
				// 定义MetriName
				metrics[i].setMetricName(metricsName[i]);
				// 定义Value
				metrics[i].setValue(10);
			}

			req.setMetrics(metrics);
			// 发送 request 请求并响应
			PutMonitorDataResponse resp = client.PutMonitorData(req);
			System.out.println(PutMonitorDataResponse.toJsonString(resp));

		} catch (TencentCloudSDKException e) {
			System.out.println(e.toString());
		}
		
	}
}
```











