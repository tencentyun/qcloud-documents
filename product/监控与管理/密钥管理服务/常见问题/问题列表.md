## 1.主密钥无法找到

主密钥只能在创建时选择的区域使用，请检查你使用的云API 访问地址是否和主密钥所在的区域的访问地址匹配，详细地域和访问地址对应列表请参考 [支持地域]()。

## 2.HTTPS限制

为了提高安全性，KMS 所有相关的API仅能通过HTTPS协议访问，如果你通过HTTP的方式访问，将会返回错误。

## 3.别名冲突

主密钥必须有别名，且每个区域内别名不可重复，当你尝试修改的主密钥别名已经在当前区域存在时，将返回错误。
