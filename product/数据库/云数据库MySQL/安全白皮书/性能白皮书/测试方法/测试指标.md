本文为您介绍云数据库 MySQL 性能测试的测试指标。

## 测试指标
- 每秒执行事务数 TPS（Transactions Per Second）
数据库每秒执行的事务数，以 COMMIT 成功次数为准。
- 每秒执行请求数 QPS（Queries Per Second）
数据库每秒执行的 SQL 数，包含 INSERT、SELECT、UPDATE、DETELE、COMMIT 等。
- 所有 event 平均耗时 avg_lat（Average Latency）

