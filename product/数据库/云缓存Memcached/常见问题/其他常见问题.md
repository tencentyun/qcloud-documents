### Memcached 最大访问能力为？
云缓存Memcached支持最大访问能力为10000次/秒/GB，如果超出服务可能会变慢。这里的容量（G）是指实际分配的容量，不是最大容量上限。
例如：实际分配的容量为50G，这50G的数据支持每秒访问次数是50*10000=500000次。

如果超过10000次/秒/GB，系统会丢弃超出的请求。
当前提供的10000次/秒/GB访问量已经能够满足大部分应用的访问需求。如果访问量超过10000次/秒/GB，请填写工单接口/端口扩容进行申请。

### 为什么 Memcached 申请的时候1GB容量的实例，但是管理控制台显示的容量却不足1GB？
- 云缓存Memcached自身需要消耗一些索引和控制元信息，这部分信息算在1GB容量以内，但是并不计费；
 - 除了索引和元信息以外，我们还会为每一个实例预留部分容量空间，以备业务突然增长时使用；
 - 索引、元信息和预留空间这三部分差不多占整个实例容量的25%，所以实例可用空间约为75%；
 - 我们只会收取75%可用空间的费用，初次购买时冻结1GB的费用，月度结算时会解冻并按照实际使用量扣费，然后再本次实际扣费进行下月的费用冻结。

### Memcached 操作是原子的吗 ?
是原子的。

### 客户端连接 Memcached 服务失败或者成功率低，与客户端所在的服务器环境有关吗？
云缓存Memcached提供了工具用于诊断客户端服务器环境以及客户端到云缓存Memcached服务之间的连接问题，详见： [云缓存Memcached连接诊断](https://cloud.tencent.com/document/product/241/3247) 。

### Redis 和 Memcached 最大的区别？
Redis支持的数据类型更丰富，支持字符串、链表、集合、有序集合、哈希表等多种数据类型。

### Memcached 的介绍？
- 腾讯云缓存Memcached，是腾讯自主研发的极高性能、内存级、持久化、分布式的Key-Value存储服务。
-  作为最终落地存储来设计的，拥有数据库级别的访问保障和持续服务能力。
-  支持Memcached协议，能力比Memcached强（能落地），适用Memcached、TTServer的地方都适用云缓存Memcached。
- 解决了内存数据可靠性、分布式及一致性上的问题，让海量访问业务的开发变得简单快捷。

### Memcached 自助清理所有数据吗？
可以：控制台—清空。
注意：
 - 数据被清空后，不可以再恢复，请在清空前确认表中的数据已经备份或不再使用。
 - 单个应用每天只能清理累计50GB的表占用空间。如果超过50GB，请 [提交工单](https://console.qcloud.com/workorder/category) 联系技术支持。
在管理中心的云缓存Memcached管理视图页面，在需要清空的的表后单击“清空”按钮，确认清空后，后台开始清空操作。清空完成后，页面会提示清空成功。

### Memcached 支持的容量空间没有上限？
云缓存Memcached支持的容量空间没有上限，只要保证金足够。
如果业务数据日增长量低于20%，云缓存Memcached能实现自动扩容，否则需要按照模板提交申请存储扩容(详情请提交工单)。

### Memcached 不提供 API ？
不提供。用户可自行下载开源的Memcached的API使用。

### Memcached 数据导入工具目前无法使用？
由于系统调整的原因，云缓存Memcached的数据导入工具目前无法使用。

如有需要，请请通过腾讯云 [提交工单](https://console.cloud.tencent.com/workorder/category)  联系我们。

### Memcached 最大访问能力为？
云缓存Memcached支持最大访问能力为10000次/秒/GB，如果超出服务可能会变慢。这里的容量（G）是指实际分配的容量，不是最大容量上限。
例如：实际分配的容量为50G，这50G的数据支持每秒访问次数是50*10000=500000次。

如果超过10000次/秒/GB，系统会丢弃超出的请求。
当前提供的10000次/秒/GB访问量已经能够满足大部分应用的访问需求。如果访问量超过10000次/秒/GB，请填写工单接口/端口扩容进行申请。

### Memcached访问说明？
关于Memcached访问，您可以 [参考文档](https://cloud.tencent.com/document/product/241/1552) 。
