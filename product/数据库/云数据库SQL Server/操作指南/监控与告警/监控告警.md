为方便用户查看和掌握实例的运行信息，云数据库 SQL Server 提供了丰富的性能监控项与便捷的监控功能（自定义视图、时间对比、合并监控项等）。
>?单个实例的表数量超过100万后，可能会影响数据库监控，请合理规范表的数量，控制单个实例表数量不超过100万。
  
## 支持监控的实例类型
云数据库 SQL Server 支持主实例、只读实例的监控，并为每个实例提供独立的监控视图供查询。

## 查看监控
1. 登录 [SQL Server 控制台](https://console.cloud.tencent.com/sqlserver)，在实例列表，单击实例 ID 或“操作”列的【管理】，进入实例管理页面。
2. 在实例管理页面，选择【系统监控】页，可查看实例各指标的监控信息。
>?
>- 云数据库 SQL Server 监控粒度支持最小10s级别。
>- 云数据库 SQL Server 目前最长支持查看180天内的监控数据。

## 监控指标
云数据库 SQL Server 监控支持 SQL Server 常见的37种参数，您可以通过配置 SSMS 的计数器，额外统计其他参数。
>?系统维度监控指标，如需查看，可通过 [在线支持](https://cloud.tencent.com/online-service?from=connect-us) 咨询。 

| 分类          | 指标中文名          | 指标英文名          | 单位                 | 指标说明           |
| ----------- | --------------------- | ------------------ | -------------------- | -------------------- |
| CPU            | CPU 利用率          | Total  Processor Time       | %                       | 实际 CPU 消耗的百分比       |
| 内存            | 内存使用          | Server  memory              | MB                    | 总内存的占用大小             |
| 最大内存     | Max memory     | KB                  | 最大内存                          |                            |
| 内存使用率    | Memory Usage           | %                    | 内存的使用率            |                              |
| 存储            | 磁盘 IOPS             | IOPS                 | 次/秒                                | 磁盘读写次数                 |
| 读取磁盘次数     | Read  IOPS          | 次/秒             | 每秒读取磁盘次数            |                              |
| 写入磁盘次数     | Write  IOPS         | 次/秒             | 每秒写入磁盘次数            |                              |
| 已用存储空间     | Storage  Space    | GB                | 实例数据库文件和日志文件占用的空间总和       |                |
| 剩余存储空间     | Free  Storage      | %                  | 硬盘剩余容量百分比，计算方式为：(购买磁盘 - 已用存储空间) / 购买磁盘 |                       |
| 读 IO 平均响应时间  | Avg. Disk Sec/Read    | ms       | 每个读 IO 的平均响应时间（系统维度监控）   |            |
| 写 IO 平均响应时间  | Avg. Disk Sec/Write   | ms       | 每个写 IO 的平均响应时间（系统维度监控）   |            |
| IO  请求平均响应时间   | Avg. Disk sec/Transfer   | ms    | 每个 IO 请求的平均响应时间（系统维度监控） |               |
| 磁盘每秒读 IO 数量        | Disk Reads/sec             | 次/秒    | 磁盘每秒的读 IO 数量（系统维度监控）     |              |
| 磁盘每秒写 IO 数量        | Disk Writes/sec            | 次/秒    | 磁盘每秒的写 IO 数量（系统维度监控）     |              |
| 磁盘 IOPS 总数            | Disk Transfers/sec         | 次/秒     | 磁盘上的 IOPS 总数（系统维度监控）       |               |
| 磁盘队列长度            | Disk Queue Length        | 个         | 磁盘队列长度  （系统维度监控）            |              |
| 网络                   | 输入流量              | Network  Receive Throughput | KB/s           | 所有连接输入包大小总和       |
| 输出流量             | Network Transmit Throughput     | KB/s            | 所有连接输出包大小总和       |                  |
| 平均网络 IO 延迟   | Network IO waits        | ms             | 平均网络 IO 延迟时间             |                              |
| 连接                   | 连接数                | User Connections  | 个          | 平均每秒用户连接数据库的个数 |
| 每秒登录次数      | Logins/sec          | 次/秒            | 每秒登录次数                  |                              |
| 每秒登出次数      | Logouts/sec        | 次/秒            | 每秒登出次数                  |                              |
| 访问                  | 慢查询                 | SlowQuery    | 个                                 | 运行时间超过一秒的查询数量   |
| 请求数               | Batch  Requests   | 次/秒            | 平均每秒的请求数           |                              |
| SQL  编译数       | SQL  Compilations/sec       | 次/秒             | 平均每秒 SQL 编译次数            |                |
| SQL  重编译数    | SQL  Re-Compilations/sec    | 次/秒           | 平均每秒 SQL 重编译次数         |                 |
| 每秒钟SQL做全表扫描数目 | Full  Scans/sec    | 次/秒                | 每秒钟SQL做全表扫描数目      |                  |
| 事务数               | Transactions/sec          | 次/秒           | 平均每秒的事务数           |                              |
| 阻塞数               | Processes  blocked      | 个                | 当前阻塞数量                  |                              |
| 执行缓存缓存命中率  | Plan  Cache  ：Cache  Hit Ratio | %    | 每个  SQL 有一个执行计划，执行计划的命中率   |        |
| 缓冲区缓存命中率   | Buffer  cache hit ratio     | %               | 数据缓存（内存）命中率         |                |
| 锁                      | 锁请求次数          | Lock  Requests/sec   | 次/秒       | 平均每秒锁请求的次数         |
| 每秒闩锁等待数量   | Latch  Waits/sec   | 次/秒               | 每秒闩锁等待数量                  |                |
| 平均锁等待延迟      | Average wait time    | Ms        | 每个导致等待的锁请求的平均等待时间     |                  |
| 死锁数                  | Number of deadlocks/sec       | 个/秒        | 每秒导致死锁的锁请求数        |                   |
| 阻塞报告生成阀值  | Blocked process threshold       | 秒        | 所指定的生成阻塞报告的阈值，超过该阈值将生成阻塞的进程报告，默认情况下，不生成阻塞的进程报告 |                              |
| 其他                    | 用户错误数         | User  Error/sec    | 次/秒             | 平均每秒错误数        |
