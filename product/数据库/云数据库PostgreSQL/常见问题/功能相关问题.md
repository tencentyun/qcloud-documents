### PostgreSQL 主要功能有哪些?
- 轻松托管部署
腾讯云可以让你在几分钟之内启动 PostgreSQL 实例并连接应用程序，而无需其他配置。默认配置具有通用性的参数，并可在管理中心参数设置实时修改。帮助你摆脱繁重和复杂的安装配置过程，提高你的运维效率。
- 便捷监控
提供了PostgreSQL的关键运行指标，包括CPU利用率、存储容量使用率、I/O 活动等性能监控数据，你可以在管理中心查看，且无需额外收费，帮助你快速定位和解决问题。自定义指标告警阈值，使你无需时刻关注监控，而可通过电子邮件或短信及时了解当前异常。
- 超高性能
腾讯云通过PCI-E SSD配置的硬盘，提供至少高于SATA三倍的QPS的配置。腾讯云CDB for PostgreSQL默认为你提供一主一备架构的部署模式，默认启动同步复制（Synchronous Replication），使你的业务不中断，避免出现的数据错乱、丢失的等问题。
- 更强保障
节点故障后，集群调度将立即开始自动重试恢复节点。当你的数据出现严重问题时，能快速恢复到某个正常时间点，以应对升级故障、灾难恢复等情况。云数据库默认为每个数据库都提供了多重安全防护，无需单独购买即可拥有。
- 可扩展
你可以通过腾讯云管理中心，实现一键升级到目标规格，而不需要你进行额外操作。升级后的实例将继承原有实例的IP和全部配置，升级过程中，仅在切换过程产生1秒的闪断，而不需要长时间停机即可随时满足业务弹性需要。现有PostgreSQL如果还是无法承载你的业务发展，仅需少量改动甚至无需改动业务，即可支撑无限容量，无瓶颈的服务海量用户。

### PostgreSQL 如何使用控制台重启实例？
重启操作经常是维护云数据库的一种必要手段，重启 PostgreSQL 实例相当于本地服务器的重启数据库（服务和进程）操作。
1.打开云数据库PostgreSQL控制台。
2.对于单个运行中的 PostgreSQL 实例，在实例列表操作栏中，单击【重启】，即可对 PostgreSQL 实例进行重启。

### PostgreSQL  如何进行系统监控？
在实例管理列表内，单击页面右侧的对应实例的“管理”按钮,
![图片描述](//bot1024-1253841380.file.myqcloud.com/d489d17ead8d11e7b152525400a3183e.png)
选择“系统监控”选项卡”，即可对相关数据进行实时监控。
![图片描述](//bot1024-1253841380.file.myqcloud.com/dfcb0828ad8d11e79c86525400a3183e.png)

### PostgreSQL 迁移数据：如何使用pg_dump命令进行逻辑备份，创建转储数据？
如果你的数据已经在PostgreSQL上，你可以psql命令将数据轻松的迁移到CDB for PostgreSQL中。
迁移数据的工作主要分为两大部分：
- 使用pg_dump命令进行逻辑备份，创建转储数据；
- 将上一步的备份数据恢复到CDB for PostgreSQL上。
准备操作：你应该已经完成PostgreSQL实例的准备，如未完成，请参见申请实例和连接实例的操作指引。
第一步
使用PostgreSQL客户端连接本地数据库，执行如下命令，备份数据。
``` 
pg_dump -U username -h hostname -p port -d databasename -f filename
``` 
参数说明如下
![图片描述](//bot1024-1253841380.file.myqcloud.com/2358e92aad8e11e79c86525400a3183e.png)
第二步
准备操作：请先将备份好的数据上传到同一内网的云服务器主机上，然后通过内网进行数据恢复，以保证网络的稳定和数据的安全。
在您登录云服务器后，在PostgreSQL客户端中，执行如下命令，恢复数据。
``` 
psql -U username -h hostname -d desintationdb -p port -f dumpfilename
``` 

参数说明如下
![图片描述](//bot1024-1253841380.file.myqcloud.com/3ec9218ead8e11e7a794525400a3183e.png)

### 如何在云服务器上恢复PostgreSQL的数据？
参考链接 [在云服务器上恢复PostgreSQL的数据](https://cloud.tencent.com/document/product/409/11642) 。

### PostgreSQL 如何创建VPC网络？
[创建VPC网络](https://cloud.tencent.com/document/product/409/7548) 并非使用云数据库的必须选项，而VPC会为您的数据库提供更强的安全性保护和灵活，您可以根据实际情况选择。本文会介绍创建VPC的方法以及功能，或许可以更好的帮助您。
第一步：创建私有网络、初始化子网和路由表。
第二步：创建子网。
第三步：新建路由表关联子网。
第四步：向子网中添加云主机。
第五步：为云主机绑定弹性 IP 访问公网。
（可选）第六步：创建安全组进行网络流量控制。

