### 1、静态网站
CloudBase Webify 不仅支持托管静态网站的各种资源（如HTML、JS、CSS、图片及媒体文件），还对开源社区内流行的开源框架进行了适配，如React、Vue、Gatsby.js、Hexo等。只需简单几步，零配置即可托管您的静态站点。

### 2、单页面应用（SPA）
传统方案下，通常需要服务端（如Nginx）配置特殊的路由逻辑，以支持单页面应用（SPA）需要的浏览器端路由。CloudBase Webify 提供自定义路由能力，支持 SPA 类型的配置，免除配置服务端的烦恼。