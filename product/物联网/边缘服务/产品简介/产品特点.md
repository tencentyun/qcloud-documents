### 按需运行
运行方式是按需运行，仅在设定的触发器上有时间产生时才运行，运行也是受调度平台的控制。

### 运行调度
调度是以一段函数代码或一个代码包来进行的，更轻量，我们需要感知到环境先换要素更少，调度平台依靠本身的能力进行运行位置的调度。

### 边缘计算
对于传统应用来说，数据再用户那产生，传到云上进行处理，再进行相应的存储。边缘计算的缩短距离实际是把处理过程更加靠近用户，靠近用户就可以认为是边缘计算的过程，并且靠近用户指的并不是加速网络速度，而是更多把计算下发，放到更靠近用户的位置。