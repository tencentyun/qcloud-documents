物联网 SaaS 托管为用户提供更适合物联网场景的一站式、高可用的 SaaS 托管服务，该服务通过流量驱动的自动弹性扩缩容能力，可有效降低用户的服务器成本与运维投入。同时支持托管任意语言和框架编写的物联网 SaaS，存量迁移几乎不需要改造代码。

## 功能特性

#### 任意语言、任意容器镜像
您可以使用任何语言、任何框架编写应用，甚至直接使用公共的容器镜像。

#### 按量计费
您的应用只有在被访问时才会进行计费。

#### 零运维，弹性伸缩
平台将会根据应用的使用情况，自动伸缩计算资源，无需人工管理。

#### 对外域名
每个服务都包含一个系统自动生成的默认域名（您也可额外绑定自定义域名），可被用于访问和传入请求。请求方无需感知该服务具体有几个版本，而是将服务视为一个整体。
#### 流量分配
当有请求传入服务时，根据您事先配置的流量分配模式（按百分比或者按 URL 参数）和规则，系统将本次请求路由到对应的版本。

#### 弹性伸缩
不同版本承载的请求数量决定它对应的实例（容器）数量，当无任何请求时，实例（容器）数量甚至可以被缩减到0，不产生任何资源消耗。这一动态扩缩过程由系统自动完成，最终您只需为实际使用的所有实例（容器）资源进行付费。

在您的实际使用中，每个服务最多可以有10个版本，每个版本的实例（容器）数量范围为0 - 50。

## 资源模型说明

资源模型中每个服务都包含两个版本，每个版本对应着一组实例（容器）资源。本文以服务 TestA、TestB 域名为例进行说明，如下图所示：
<img src="https://main.qcloudimg.com/raw/2d449c076b4162bf42345efa584c3c53.png" style="width: 718px;"></img>
