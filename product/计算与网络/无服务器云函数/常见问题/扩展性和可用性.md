### 云函数的可用性如何？

SCF 云函数通过跨地域部署、复制和冗余来提供高可用性。

### 改动代码或配置时，云函数还可以使用吗？

可以。更新函数时会有短暂的窗口期，通常不到 1 分钟，在这期间请求将由旧函数代码或新函数代码实现。

### 单次运行的函数数量有没有限制？

SCF 可以支持大量函数实例并行。但是，我们有一项默认的安全阈值：每个函数最多 100 个并发执行量。此阈值可以通过提交工单进行提升。

### 函数在处理事件时发生故障会怎样？

如遇故障，进行同步调用的函数会返回异常信息。异步调用的函数将在后台自动重试 3 次。
