本文将介绍什么是 VPC 型专线网关和云联网专线网关的路由表，以及各方向路由的转发原则。

## 什么是专线网关路由表

### VPC 型专线网关
在 VPC 型专线网关的专线网络架构中，专线网的路由表中包含 IDC 方向和 VPC 方向的路由策略。
 > ? 专线接入控制台暂时不支持查看 VPC 型专线网关的路由表。
 > 
<table>
<tr>
<th width="20%">路由方向</th>
<th width="10%">路由模式</th>
<th width="70%">说明</th>
</tr>
<tr>
<tr>
<td rowspan="2"><a href="#idc">IDC 方向的路由</a></td>
<td>BGP</td>
<td>若专用通道为 BGP 模式，则专线网关通过 BGP 协议学到 IDC 方向的路由策略。</td>
</tr>
<tr>
<td>静态</td>
<td>若专用通道为静态模式，则专线网关通过在专用通道 IDC 网段录入路由信息，学习到 IDC 方向的路由策略。</td>
</tr>
<tr>
<td >VPC 方向的路由</td>
<td>静态</td>
<td>通过在 VPC 路由表控制台中手动录入路由信息，学习到 VPC 方向路由策略。由于一个 VPC 型专线网关仅可绑定一个 VPC，因此仅存在一条路由策略。</td>
</tr>
</table>

例如专线网络架构中，VPC CIDR 为 172.21.0.0/20，IDC 路由为192.168.0.0/24，则专线网关路由表的 IDC 方向和 VPC 方向路由如下图所示。
![](https://main.qcloudimg.com/raw/cd684885f99a856a045f3dc6841d8781.png)

### 云联网专线网关
在云联网型专线网关的专线网络架构中，专线网的路由表包含云联网方向和 IDC 方向的路由策略。
 > ? 您可以在控制台查看云联网专线网关的路由表，具体操作请参见[ 查看路由表]()。
 > 
<table>
<tr>
<th>路由方向</th>
<th>路由模式</th>
<th>说明</th>
</tr>
<tr>
<tr>
<td rowspan="2"><a href="#idc">IDC 方向的路由</a></td>
<td>BGP</td>
<td>若专用通道为 BGP 模式，则专线网关通过 BGP 协议学到 IDC 方向的路由策略。</td>
</tr>
<tr>
<td>静态</td>
<td>若专用通道为静态模式，则专线网关通过在专用通道 IDC 网段录入路由信息，学习到 IDC 方向的路由策略。</td>
</tr>
<tr>
<td ><a href="#yun">云联网方向的路由</a></td>
<td>BGP</td>
<td>云联网自动下发路由策略，专线网关通过 BGP 协议学习。</td>
</tr>
</table>

例如专线网络架构中，子网 CIDR 为 172.21.0.0/20，IDC 路由为192.168.0.0/24，则专线网关路由表的 IDC 方向和云联网方向路由如下图所示。
![](https://main.qcloudimg.com/raw/471199ef14fbd2ed8425b1504b0e614e.png)

## [IDC 方向的路由](id:idc)
若同一个云联网专线网关的 IDC 方向的路由表中同时存在多个路由策略时，则路由转发原则如下：
![](https://main.qcloudimg.com/raw/69430ca3f89093f87d88c6cf81a36204.png)

**示例**
若云联网专线网关的 IDC 方向的路由表中有10.0.0.0/24（BGP，AS-Path 65102）、11.0.0.0/24（静态）、192.168.254.0/24（BGP，AS-Path 18932）、172.16.1.10/32（BGP，AS-Path 18932）等路由策略，则转发11.0.0.0/24路由策略。

## [云联网方向的路由](id:yun)
若同一个云联网专线网关的云联网方向的路由表中同时存在多个路由策略时，则路由转发原则如下：
![](https://main.qcloudimg.com/raw/f9f5a282477c93f96bf94c40591d1e2d.png)

**示例**
若云联网专线网关的云联网方向的路由表中有192.168.0.0/16（AS-Path 65102）、172.26.0.16/28（AS-Path 18932）、172.16.0.0/16（AS-Path 18932）等路由策略，则转发172.26.0.16/28路由策略。
