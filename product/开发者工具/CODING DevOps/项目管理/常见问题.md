### 谁可以看到迭代列表？
项目内所有成员都可查看。

### 迭代的顺序是怎么安排的？
迭代阶段按先后顺序分别为：未开始、进行中和已完成，且进入到每个阶段后不可回撤到上一个阶段。

### 事务有哪几种状态？
事务状态指事务的三大基本状态类型分别为：未开始、进行中和已完成。

### 迭代阶段顺序是怎样变更的？
迭代阶段按先后顺序分别为：未开始、进行中和已完成，且进入到每个阶段后不可回撤到上一个阶段。

### 默认需求包含哪些状态？
需求默认包含 7 个状态，依次是：未激活、已计划、设计中、 研发中、测试中、已发布、已关闭。